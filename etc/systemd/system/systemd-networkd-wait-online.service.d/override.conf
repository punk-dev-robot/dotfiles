[Service]
# Clear the original ExecStart
ExecStart=
# Wait for ANY interface to be online, with 30s timeout (matching NetworkManager's old config)
ExecStart=/usr/lib/systemd/systemd-networkd-wait-online --any --timeout=30