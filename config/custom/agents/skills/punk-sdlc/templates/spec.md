# <KEY> — spec

## What we build
Two or three sentences. `fast` flow: this is the whole spec.

## Assumptions
Each unresolved assumption as options, with the one I recommend marked:

- **A1 — <question>**: (a) <option> — <consequence>; (b) <option> — <consequence>.
  **Default: (a)** because <reason>. Overturn by saying so at the gate.

## Acceptance criteria
- **AC1** — <observable statement>. **Verifier:** <concrete check: query returns 0 rows, p95 <
  300 ms, test name, screenshot>.

## Out of scope
What this ticket deliberately does not do, and where it goes instead.

## Sign-off
`<date>` — approved at the spec gate by <who>, with <changes made>.
