# Onboarding guide — dotfiles

Generated at: 2026-04-15  |  Developer: developer  |  Level: junior

## What this project is

Could not find `memory.md` — continuing without it. This guide is based only on the onboarding artifacts that were available.

The repository folder is named `dotfiles`, but no project memory file was available to confirm a fuller onboarding summary. No feature specs or task files were found under `features/`, so there is no active feature history to summarize yet.

## How the workflow works here

Could not find `.speckit/extensions.json` — continuing with an empty extension list. Because of that, no extension-specific SDD flow, hooks, or automatic validations could be confirmed from the required onboarding artifacts.

At the moment, treat the workflow conservatively: if feature artifacts appear later, read the feature spec first, then the task list, then implement the smallest next task, and finally run whatever validations are required by the part of the project you changed. No project-specific post-implementation hook sequence could be confirmed from the required onboarding inputs.

## Features in progress

No features found. The project is in its initial phase.

## Where to start

1. N/A · Read `README.md` and `CLAUDE.md` — project orientation
   Why: As a junior developer, the fastest safe start is understanding the repo purpose and any explicit working rules before touching files.
2. N/A · Inspect `.dotter/global.toml` — deployment map
   Why: This shows how managed files are connected to deployed locations, which is usually the most important structural map in a dotfiles repo.
3. N/A · Review `config/`, `etc/`, and `local/` — repository boundaries
   Why: These directories usually separate user config, system config, and helper scripts, which helps you avoid making changes in the wrong place.
4. N/A · Re-run onboarding with your own name — profile setup
   Why: Using `/onboard start --dev "your-name"` gives you a personal profile without overwriting anyone else’s progress.

## Project glossary

No project-specific SDD terms were detected in the required onboarding artifacts because `memory.md`, `.speckit/extensions.json`, and `features/*` artifacts were not found.

## Active extensions and how they affect your day-to-day

| Extension | Day-to-day effect |
|-----------|-------------------|
| None detected | Could not find `.speckit/extensions.json`, so no installed extension behavior could be confirmed from the required onboarding artifacts. |
